{
  "rules": {
    "smartshop": {
      "users": {
        "$uid": {
          ".read": "auth != null && auth.uid === $uid",
          ".write": "auth != null && auth.uid === $uid"
        }
      },
      "global": {
        ".read": "auth != null",
        ".write": "auth != null"
      },
      "$listId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    }
  }
}
